spring:
  application:
    name: patient-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.5
        initial-interval: 1000
